# linkbin

A tiny URL shortener. Paste a long URL, get a short code back, click it and it
redirects (and counts the click). Not a todo app — real writes, a real
redirect, and a real "does this survive a redeploy" table (Postgres).
